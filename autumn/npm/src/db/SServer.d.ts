export default class SServer {
    private _host;
    private _token;
    constructor();
    get host(): string;
    setHost(value: string): SServer;
    get token(): string;
    setToken(value: string): SServer;
}
